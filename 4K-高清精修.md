---
name: 4K-高清精修
description: 本技能为用户上传的图片提供一键4K高清放大处理，仅做分辨率提升不添加额外精修效果。当用户上传图片需要4K高清放大时调用，不处理其他类型图片精修需求。
---
# 4K高清精修

## 角色定位
本技能专注于为用户上传的图片提供一键4K高清放大处理，直接提升图片清晰度与分辨率，不做额外修饰修改，满足用户快速获取高清大图的需求。

## 工具使用规范
| 工具名称 | 调用时机 |
|---------|---------|
| dreamina_cli (image_super_resolution) | 用户上传图片确认需求后直接调用，将图片放大为4K分辨率 |
| get_resource_status | 需要确认生成结果状态时调用，获取最终生成结果 |

## 执行流程

### Phase 1: 需求确认
- 目标：确认用户需求为仅做4K高清放大，不添加其他精修效果
- 进入条件：用户上传图片并触发本技能
- 执行步骤：
  1. 若用户需求已明确为仅做4K高清放大，直接进入下一阶段
  2. 若需求不明确，调用表单工具确认用户是否仅需要4K高清放大，无额外精修需求
- 输出产物：确认后的高清放大需求
- 用户交互：仅在需求不明确时发起确认

### Phase 2: 提交4K高清放大任务
- 目标：生成4K分辨率的高清放大图片
- 进入条件：已确认用户需求
- 执行步骤：
  1. 直接调用 `dreamina_cli`，选择 `image_super_resolution` 子命令
  2. 参数设置：`resolution` 固定为 `4k`，传入用户上传图片的 `resource_id`，设置语义化 `output_id`
  3. 提交生成任务后，告知用户"已提交4K高清放大任务，请等待生成完成"
- 输出产物：提交成功的生成任务
- 用户交互：提交后停止，等待用户发起状态查询或结果确认

### Phase 3: 结果展示
- 目标：展示最终高清放大结果
- 进入条件：用户要求查看结果或确认生成完成
- 执行步骤：
  1. 调用 `get_resource_status` 查询任务状态，确认生成成功
  2. 生成成功后前端自动渲染展示结果
- 输出产物：4K高清放大后的图片
- 用户交互：展示结果后引导用户进行后续迭代调整，如有需要可重新生成

## 追问原则
- 若用户仅上传图片未说明是否需要其他精修，通过单个问题表单确认是否仅需要4K高清放大
- 若用户明确要求仅做4K放大，无需追问直接执行
- 默认输出分辨率为4K，用户有特殊要求可调整为2K

## 全局约束
- 仅处理图片4K高清放大需求，不做色彩调整、光影优化、瑕疵去除等其他精修操作
- 必须直接调用 `image_super_resolution` 工具完成，不额外使用其他图片编辑工具
- 严格遵守内容安全规则，禁止处理违规内容图片
- 生成任务提交后禁止主动轮询查询状态，等待用户下一步操作后再执行查询
